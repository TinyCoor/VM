%include "../examples/vm_lib.hasm"
jmp main
%label N 69420
main:
    push N
loop:
    dup 0
    push 1
    and
    ffi print_u64

    push 1
    shr

     dup 0
     push 0
     eq
     not

    jmp_if loop

    halt
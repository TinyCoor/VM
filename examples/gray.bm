%include "./examples/vm_lib.hasm"

%bind N 30
jmp main

main:
    push 0
loop:
    dup 0
    dup 0
    push 1
    shr
    xor
    ffi print_u64

    push 1
    addi

    dup 0
    push N
    eq
    not

    jmp_if loop
    halt